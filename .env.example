# Classic PAT, no scopes needed for public repos. Lifts rate limit 60 -> 5000/hr.
GITHUB_TOKEN=

# Only needed for the LLM layer (classify + synthesize). The rule layer works without it.
# Google AI Studio key (free tier): https://aistudio.google.com/apikey
GEMINI_API_KEY=
